import { NotFoundScreen } from '@/components';

export default function AppNotFound() {
  return (
    <NotFoundScreen
      title="Page not found"
      message="The page you’re looking for doesn’t exist (or it has been moved)."
    />
  );
}
